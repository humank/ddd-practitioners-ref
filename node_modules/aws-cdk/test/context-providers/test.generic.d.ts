import { Test } from 'nodeunit';
declare const _default: {
    'errors are reported into the context value'(test: Test): Promise<void>;
    'errors are marked transient'(test: Test): Promise<void>;
};
export = _default;
