import { Test } from 'nodeunit';
declare const _default: {
    'zipDirectory can take a directory and produce a zip from it'(test: Test): Promise<void>;
    'md5 hash of a zip stays consistent across invocations'(test: Test): Promise<void>;
    'zipDirectory follows symlinks'(test: Test): Promise<void>;
};
export = _default;
