import cxapi = require('@aws-cdk/cx-api');
import { ISDK } from '../api';
import { ContextProviderPlugin } from './provider';
export declare class VpcNetworkContextProviderPlugin implements ContextProviderPlugin {
    private readonly aws;
    constructor(aws: ISDK);
    getValue(args: cxapi.VpcContextQuery): Promise<cxapi.VpcContextResponse>;
    private findVpc;
    private readVpcProps;
}
